#include "monty.h"
int main(int argc, char **argv)
{
	FILE *
	if (argc != 2)
	{
		fprintf(stderr, "USAGE: monty file\n");
		exit(EXIT_FAILURE);
	}
	if ()
	{
		printf("Error: Can't open file <file>\n");
		exit(EXIT_FAILURE);
	}
}
